package main
import "fmt"
func main() {
   var n int
   if _, err := fmt.Scan(&n); err != nil {
     return
   }

   melhorTrilha := 1
   menorEsforco := 0

   for i := 1; i <=j; i++{
    var m int
    fmt.Scan(&m)

    h := make([]int, m)
    for t := 0; t < m; t++{
        fmt.Scan(&h[t])
    }

    subidaIda := 0
   }
    fmt.Println("Hello, World!")
}